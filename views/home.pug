html
    body
        h1 Course Management System
        h3 Home
        h4 Welcome #{name} (#{auth_token})!
        span #{message}
        if admin
            span ADMIN
        else
            span STUDENT